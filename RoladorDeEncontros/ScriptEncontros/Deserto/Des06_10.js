function Des06_10(){
    eval(document.scripts[1].outerText);    //variáveis básicas

    str = "1d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(0, 1, drv.soma + " "+grmtc.plu(drv.soma, 'batedores'));

    str = "2d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(1, 1,drv.soma + " "+grmtc.plu(drv.soma, 'homem-chacais'));

    str = "2d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(2, 1,drv.soma + " "+grmtc.plu(drv.soma, 'hobgoblins'));

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(3, 1,(drv.soma +3) + " "+grmtc.plu((drv.soma +3), 'mephits')+" da poeira");

    str = "1d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(4, 1,drv.soma +" "+ grmtc.plu(drv.soma, 'enxames')+" de insetos");

    tab.splice(5, 1,"1 cobra constritora gigante");

    tab.splice(6, 1,"1 leão");

    str = "2d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(8, 1,drv.soma + " "+grmtc.plu(drv.soma, 'gnolls'));

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(10, 1,drv.soma + " "+grmtc.plu(drv.soma, 'sapos')+" "+grmtc.plu(drv.soma, 'gigantes'));

    tab.splice(12, 1,"1 múmia");

    str = "1d8";
    eval(rs+" "+functionEvalName+";");
    tab.splice(17, 1,(drv.soma +1) + " "+grmtc.plu((drv.soma +1), 'abutres')+" "+grmtc.plu((drv.soma+1), 'gigantes'));

    tab.splice(20, 1,"Um obelisco de pedra parcialmente enterrado na areia");

    str = "1d3";
    eval(rs+" "+functionEvalName+";");
    tab.splice(25, 1,"1 ogro com "+drv.soma+" "+grmtc.plu(drv.soma, 'meio-ogros'));

    str = "1d10";
    eval(rs+" "+functionEvalName+";");
    tab.splice(28, 1,drv.soma + " "+grmtc.plu(drv.soma, 'hienas')+" "+grmtc.plu(drv.soma, 'gigantes'));

    str = "1d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(35, 1,(drv.soma+1) + " "+grmtc.plu((drv.soma+1), 'tendas')+" "+grmtc.plu((drv.soma+1), 'vazias'));

    str = "1d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(40, 1,(drv.soma+2)+" thri-kreen");

    str = "2d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(43, 1,drv.soma + " yuan-ti puro sangue");

    str = "1d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(46, 1,(drv.soma+3)+" "+grmtc.plu((drv.soma+3),"cães")+" da morte");

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(50, 1,drv.soma + " "+grmtc.plu(drv.soma, 'escorpiões')+" "+grmtc.plu(drv.soma, "gigantes"));

    tab.splice(52, 1,"1 elemental do fogo");

    str = "3d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(53, 1,"1 hobgoblin capitão com "+drv.soma + " "+grmtc.plu(drv.soma, 'hobgoblins'));

    str = "1d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(55, 1,(drv.soma+2)+" "+grmtc.plu((drv.soma+2), 'ogros'));

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(56, 1,drv.soma+" "+grmtc.plu(drv.soma, 'lâmias'));

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(58, 1,"1 elemental do ar");

    tab.splice(60, 1,"Um meteorito em repouso no fundo de uma cratera vítrea");

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(65, 1,(drv.soma+1)+" "+grmtc.plu((drv.soma+1), 'inumanos'));

    tab.splice(66, 1,"1 dragão de bronze jovem");

    str = "1d3";
    eval(rs+" "+functionEvalName+";");
    drv2 = drv.soma;
    str = "3d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(68, 1,"1 capitão dos bandidos com "+drv2+" "+grmtc.plu(drv2, "furiosos")+" e "+drv.soma+" "+grmtc.plu(drv.soma, "bandidos"));

    tab.splice(70, 1,"1 ciclope");

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(72, 1,drv.soma + " "+grmtc.plu(drv.soma, 'couatls'));

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(73, 1,drv.soma+" yuan-ti mestiço");

    str = "1d3";
    eval(rs+" "+functionEvalName+";");
    drv2 = drv.soma;
    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(75, 1,"Ventos fortes que atiram areia e reduzem a visibilidade para "+drv2+" "+grmtc.plu(drv2, 'metros')+" por "+drv.soma+" "+grmtc.plu(drv.soma, 'horas'));

    str = "1d3";
    eval(rs+" "+functionEvalName+";");
    tab.splice(80, 1,"1 ressurgido com "+drv.soma+" "+grmtc.plu(drv.soma, 'inumanos'));

    str = "1d8";
    eval(rs+" "+functionEvalName+";");
    tab.splice(83, 1,(drv.soma +1) + " "+grmtc.plu((drv.soma +1), 'aranhas')+" "+grmtc.plu((drv.soma +1), 'interplanares'));

    str = "1d8";
    eval(rs+" "+functionEvalName+";");
    tab.splice(85, 1,(drv.soma +2)+" "+grmtc.plu((drv.soma+2), 'homens')+"-tigre");

    str = "2d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(87, 1,drv.soma + " "+grmtc.plu(drv.soma, 'gnolls')+" presa de Yeenoghu");

    tab.splice(90,1,"Um dragão azul jovem");

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(91,1,drv.soma+" "+grmtc.plu(drv.soma, 'ciclopes'));

    str = "1d3";
    eval(rs+" "+functionEvalName+";");
    tab.splice(92,1, drv.soma+" yuan-ti abominação");

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(93, 1, drv.soma + " "+grmtc.plu(drv.soma, 'medusas'));

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(94,1, "1 naga guardiã");

    str = "1d3";
    eval(rs+" "+functionEvalName+";");
    tab.splice(95,11, drv.soma+" "+grmtc.plu(drv.soma , 'dragões')+" de bronze "+grmtc.plu(drv.soma , 'jovens'));   //Yuan-ti não tem plural

    tab.splice(96,1, "1 efreeti");

    tab.splice(97,1, "1 roca");

    tab.splice(98,1, "1 ginoesfinge");

    tab.splice(99,1, "1 dragão de bronze adulto");

    document.getElementById("desOut").innerHTML = dado +" &rarr; (";
    for(var i = 0; i < tab.length; i++){
        if(dado == i){
            document.getElementById("desOut").innerHTML += dado + ". ";
            if(tab[i] != "-"){
                document.getElementById("desOut").innerHTML += "): "+ '<br>' + tab[i];
            }
            else if(tab[i] == "-"){
                dado -= 1;
                i -= 2;
            }
        }
    }
}
