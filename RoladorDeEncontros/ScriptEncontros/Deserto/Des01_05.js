function Des01_05() {

    eval(document.scripts[1].outerText);    //variáveis básicas

    str = "3d8";
    eval(rs+"; "+functionEvalName/*+";"*/);
    tab.splice(0, 1, drv.soma + " "+grmtc.plu(drv.soma, 'escorpiões'));

    str = "2d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(1, 1,drv.soma + " "+grmtc.plu(drv.soma, 'abutres'));

    tab.splice(2, 1,"1 mula abandonada ");

    str = "2d6";
    eval(rs+" "+functionEvalName+";");
    drv2 = drv.soma;
    str = "2d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(3, 1,drv2 + " "+grmtc.plu(drv2, 'plebeus')+" com "+ drv.soma + " "+grmtc.plu(drv.soma, 'camelos')+" destinados à uma cidade distante ");

    str = "1d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(4, 1,drv.soma + " "+grmtc.plu(drv.soma, 'cobras')+" "+grmtc.plu(drv.soma, 'voadoras'));


    str = "2d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(5, 1,drv.soma + " "+grmtc.plu(drv.soma, 'hienas')+" ou "+ drv.soma +" "+grmtc.plu(drv.soma, 'chacais'));   //Ou

    str = "1d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(6, 1,drv.soma + " "+grmtc.plu(drv.soma, 'guardas')+" escoltando um nobre até a beira do deserto, todos eles montados em camelos");

    str = "1d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(7, 1,drv.soma + " "+grmtc.plu(drv.soma, 'gatos'));

    tab.splice(8, 1,"1 pseudodragão ");

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(9, 1,drv.soma + " "+grmtc.plu(drv.soma, 'cobras')+" "+grmtc.plu(drv.soma, 'venenosas'));

    str = "2d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(10, 1,drv.soma + " "+grmtc.plu(drv.soma, 'stirges'));

    str = "1d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(13, 1,(drv.soma +2) + " aranha-lobo "+grmtc.plu((drv.soma +2), 'gigantes'));

    tab.splice(15, 1,"1 batedor ");

    str = "2d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(17, 1,drv.soma + " "+grmtc.plu(drv.soma, 'serpentes')+" "+grmtc.plu(drv.soma, 'venenosas')+" "+grmtc.plu(drv.soma, 'gigantes'));

    tab.splice(20, 1,"Pegadas em fila única em direção a dentro do deserto ");

    str = "4d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(25, 1,drv.soma + " "+grmtc.plu(drv.soma, 'kobolds'));

    tab.splice(27, 1,"1 homem-chacal ");

    str = "3d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(29, 1,drv.soma + " "+grmtc.plu(drv.soma, 'guerreiros')+" "+grmtc.plu(drv.soma, 'tribais'));

    str = "1d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(31, 1,drv.soma + " "+grmtc.plu(drv.soma, 'lagartos')+" "+grmtc.plu(drv.soma, 'gigantes'));

    tab.splice(33, 1,"1 enxame de insetos ");

    tab.splice(35, 1,"Um oasis rodeado de palmeiras e contendo os restos de um velho acampamento ");

    str = "3d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(40, 1,drv.soma + " "+grmtc.plu(drv.soma, 'bandidos'));

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(44, 1,drv.soma + " "+grmtc.plu(drv.soma, 'cobras')+" "+grmtc.plu(drv.soma, 'constritoras'));

    str = "2d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(46, 1,drv.soma + " "+grmtc.plu(drv.soma, 'kobolds')+" "+grmtc.plu(drv.soma, 'alados'));

    tab.splice(48, 1,"1 mephit da poeira ");

    str = "1d3";
    eval(rs+" "+functionEvalName+";");
    tab.splice(50, 1,(drv.soma +1) + " "+grmtc.plu((drv.soma +1), 'sapos')+" "+grmtc.plu((drv.soma +1), 'gigantes'));

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(52, 1,drv.soma + " "+grmtc.plu(drv.soma, 'aranhas')+" "+grmtc.plu(drv.soma, 'gigantes'));

    tab.splice(54, 1,"1 druida ");

    str = "2d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(55, 1,drv.soma + " "+grmtc.plu(drv.soma, 'hobgoblins'));

    tab.splice(57, 1,"1 inumano ");

    tab.splice(58, 1,"1 ogro");

    tab.splice(60, 1,"Uma lâmpada de latão caída no chão ");

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(65, 1,drv.soma + " "+grmtc.plu(drv.soma, 'abutres')+" "+grmtc.plu(drv.soma, 'gigantes'));

    tab.splice(67, 1,"1 aranha interplanar ");

    tab.splice(68, 1,"1 cobra constritora gigante ");

    str = "1d3";
    eval(rs+" "+functionEvalName+";");
    tab.splice(69, 1,"1 gnoll líder de matilha com "+drv.soma+" "+grmtc.plu(drv.soma, 'hienas')+" "+grmtc.plu(drv.soma, 'gigantes'));

    str = "1d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(71, 1,(drv.soma +2) + " "+grmtc.plu((drv.soma +2), 'gnolls'));

    tab.splice(72, 1,"1 múmia");

    str = "1d3";
    eval(rs+" "+functionEvalName+";");
    tab.splice(74, 1,drv.soma + " "+grmtc.plu(drv.soma, 'meio-ogros'));

    tab.splice(75,1,"Uma pilha de ossos humanóides enrolados em panos apodrecidos");

    tab.splice(80,1,"lâmia");

    str = "2d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(82,1, "hobgoblin capitão com "+drv.soma+" "+grmtc.plu(drv.soma, 'hobgoblins'));

    str = "2d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(83, 1, drv.soma + " "+grmtc.plu(drv.soma, 'cães')+" da morte");

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(84,1, drv.soma + " "+grmtc.plu(drv.soma, 'escorpiões')+" "+grmtc.plu(drv.soma, 'gigantes'));

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(86,11, "1 yuan-ti mestiço com "+(drv.soma +1)+" yuan-ti puro-sangue");   //Yuan-ti não tem plural

    str = "3d6";
    eval(rs+" "+functionEvalName+";");
    tab.splice(87,1, "1 capitão dos bandidos com 1 druida e "+drv.soma+" "+grmtc.plu(drv.soma, 'bandidos'));

    str = "2d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(89,1, drv.soma + " thri-kreen");

    tab.splice(90,1, "1 elemental do ar");

    str = "1d3";
    eval(rs+" "+functionEvalName+";");
    tab.splice(91,1, drv.soma + " "+grmtc.plu(drv.soma, 'couatls'));

    tab.splice(92,1, "1 elemental do fogo");

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(93,1, drv.soma + " "+grmtc.plu(drv.soma, 'gnolls')+" presa de Yeenoghu");

    tab.splice(94,1, "1 ressurgido");

    str = "1d4";
    eval(rs+" "+functionEvalName+";");
    tab.splice(95,1, drv.soma + " "+grmtc.plu(drv.soma, 'homens')+"-tigre");

    tab.splice(96,1,  "1 ciclope");

    tab.splice(97,1, "1 dragão de latão jovem");

    tab.splice(98,1, "1 medusa");

    tab.splice(99,1, "1 yuan-ti abominação");
    //---

    //console.log(tab);
    document.getElementById("desOut").innerHTML = dado +" &rarr; (";
    for(var i = 0; i < tab.length; i++){
        if(dado == i){
            document.getElementById("desOut").innerHTML += dado + ". ";
            if(tab[i] != "-"){
                document.getElementById("desOut").innerHTML += "): "+ '<br>' + tab[i];
            }
            else if(tab[i] == "-"){
                dado -= 1;
                i -= 2;
            }
        }
    }
}
